"""Top-level package namespace for repository Python code.

This repo primarily holds tooling and multi-language components. Place
shared Python modules under `src/` and import using the configured
`pythonpath` from `pyproject.toml`.
"""

